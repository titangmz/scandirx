# Include the README and LICENSE files
include README.md
include LICENSE

# Include all C source files and headers
recursive-include scandirx *.c *.h

# Include any additional package data (if applicable)
# For example, include data files in the package
# recursive-include scandirx/data *.dat *.json

# Exclude build directories and compiled files
global-exclude *.pyc *.pyo __pycache__ *.so
